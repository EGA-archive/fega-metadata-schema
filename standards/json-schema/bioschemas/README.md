This directory contains modified work from BioSchemas and the BioThings Data Discovery Engine. See licensing details at [LICENSE](./LICENSE).

FEGA adaptations preserve the upstream schemas' intent while aligning them with this repository's validation environment. In particular, the local copies convert draft-07-style single-schema array `items` declarations into draft 2020-12-compatible homogeneous `items` schemas.
