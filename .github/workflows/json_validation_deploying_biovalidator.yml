# ----------------------------------------------------------------------
# Validate FEGA metadata
#
# Goal
# ----
# - Launch a local Biovalidator instance that uses **the current branch's**
#   JSON Schemas (`./schemas/**/schema.json`).
# - Validate valid examples to confirm they remain valid.
# - Validate invalid examples to confirm they fail (as expected).
#
# What it catches
# ---------------
# 1. Schemas that no longer compile or reference each other correctly.
# 2. Valid examples that would fail against changes in the schemas.
# 3. Invalid examples that unexpectedly pass validation.
#
# Outcome
# -------
# - Fails the job (and therefore blocks the merge) if:
#     – Biovalidator cannot start
#     – Valid examples do not pass JSON Schema validation
#     – Invalid examples do not fail JSON Schema validation
# - Always uploads the validation summary for inspection.
# ----------------------------------------------------------------------
name: |
  [REQUIRED] Validate FEGA (meta)data (deploying local biovalidator)

on:
  pull_request:
    paths:
      - '**/*.json'
      - 'schemas/**'
      - 'standards/**'
      - 'scripts/**'
      - 'src/**'
      - 'pyproject.toml'
      - 'requirements.txt'
      - '.github/workflows/**'
  push:
    branches: [ dev, main ]

  workflow_dispatch:
    inputs:
      schemas_ref:
        description: |
          Specify the branch (e.g., dev), tag (e.g., v1.3.11) or commit (e.g., 5fad895) to use as source (both JSON Schemas and JSON documents) for validation
        type: string
        required: true
        default: dev

jobs:
  validate-json-docs:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: "Checkout (trigger: PR / push)"
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: actions/checkout@v5

      - name: "Checkout (trigger: manual run)"
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.schemas_ref }}   # The value given at the "Run workflow" form as a string

      # --------------------------------------------------------------
      # Environment: cache Python wheels & npm packages for speed
      # --------------------------------------------------------------
      - name: Cache pip
        uses: actions/cache@v5
        with:
          # On subsequent runs, if the cache key matches (i.e., operating system and the hash of the requirements.txt file),
          #   the dependencies are restored from the cache instead of being downloaded and installed again.
          path: ~/.cache/pip
          # combines the OS type and a hash of the requirements.txt file: the cache is specific to the dependencies listed
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
            
      # --------------------------------------------------------------
      # Tooling: Node (Biovalidator) + Python
      # --------------------------------------------------------------
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Biovalidator (EbiEga main branch)
        run: |
          # Use the EGA maintained 'main' branch of EbiEga's fork.
          npm install -g "github:EbiEga/biovalidator#main"
          # Consider auditing the issues if there are lots:
          # npm audit fix

      - name: Launch Biovalidator
        run: |
          node "$(npm root -g)/biovalidator/src/biovalidator.js" \
            --port 3020 \
            --ref "./schemas/**/schema.json" \
            --ref "./schemas/**/*.schema.json" \
            --ref "./standards/json-schema/**/*.json" \
            > biovalidator.log 2>&1 &
          echo $! > biovalidator.pid

      - name: Wait for Biovalidator to be ready
        run: |
          for i in {1..12}; do
            if curl --output /dev/null --silent --head --fail "http://localhost:3020/validate"; then
              echo "Biovalidator is up"
              exit 0
            fi
            echo "Waiting ($i/12 time(s)) for Biovalidator server to start..."
            sleep 5
          done
          echo "Biovalidator did not start in time" >&2
          exit 1

      - name: Show Biovalidator log (on failure)
        if: failure()
        run: |
          echo "=== biovalidator.log ==="
          if [ -f biovalidator.log ]; then
            cat biovalidator.log
          else
            echo "'biovalidator.log' not found"
          fi

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          # We'll use the latest version of python from major 3 release
          python-version: '3.x'
          cache: 'pip'

      - name: Install Python requirements
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install repo in editable mode
        run: pip install -e .[dev]

      # --------------------------------------------------------------
      # Validation
      # --------------------------------------------------------------
      - name: Validate examples
        id: validate_entity_examples
        continue-on-error: true
        run: |
          python scripts/py/validate_examples.py \
            --root schemas/entities \
            --url "http://localhost:3020/validate" \
            --summary-dir validation-summaries/entity-examples \
            -v

      - name: Validate graph examples
        id: validate_graph_examples
        continue-on-error: true
        run: |
          python scripts/py/validate_examples.py \
            --root schemas \
            --entity graph \
            --url "http://localhost:3020/validate" \
            --summary-dir validation-summaries/graph-examples \
            -v

      - name: Validate JSON-LD frame round-trips
        id: validate_jsonld_frame_round_trips
        continue-on-error: true
        run: |
          python scripts/py/validate_jsonld_frames.py \
            --root schemas/entities \
            --url "http://localhost:3020/validate" \
            --summary-dir validation-summaries/entity-frames \
            -v

      - name: Validate graph JSON-LD frame round-trips
        id: validate_graph_jsonld_frame_round_trips
        continue-on-error: true
        run: |
          python scripts/py/validate_jsonld_frames.py \
            --root schemas \
            --entity graph \
            --url "http://localhost:3020/validate" \
            --summary-dir validation-summaries/graph-frames \
            -v

      - name: Show summary in log
        if: always()
        run: |
          echo "=== Entity JSON Schema Example Validation ==="
          if [ -f validation-summaries/entity-examples/summary.json ]; then
            jq '.' validation-summaries/entity-examples/summary.json
          else
            echo "'entity_summary.json' not found"
          fi
          echo ""
          echo "=== Graph JSON Schema Example Validation ==="
          if [ -f validation-summaries/graph-examples/summary.json ]; then
            jq '.' validation-summaries/graph-examples/summary.json
          else
            echo "'graph_summary.json' not found"
          fi
          echo ""
          echo "=== Entity JSON-LD Frame Round-Trip Validation ==="
          if [ -f validation-summaries/entity-frames/frame_summary.json ]; then
            jq '.' validation-summaries/entity-frames/frame_summary.json
          else
            echo "'entity_frame_summary.json' not found"
          fi
          echo ""
          echo "=== Graph JSON-LD Frame Round-Trip Validation ==="
          if [ -f validation-summaries/graph-frames/frame_summary.json ]; then
            jq '.' validation-summaries/graph-frames/frame_summary.json
          else
            echo "'graph_frame_summary.json' not found"
          fi

      # --------------------------------------------------------------
      # Artifacts & teardown
      # --------------------------------------------------------------
      # We upload the artifact so we can download the validation summary
      #   even after the logs disappear, and any other tool can access it
      #   easily.
      - name: Upload validation summary
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: json-validation-summary
          path: |
            validation-summaries/entity-examples/summary.json
            validation-summaries/graph-examples/summary.json
          if-no-files-found: warn

      - name: Upload frame validation summary
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: frame-validation-summary
          path: |
            validation-summaries/entity-frames/frame_summary.json
            validation-summaries/graph-frames/frame_summary.json
          if-no-files-found: warn

      - name: Stop Biovalidator
        if: always()
        run: |
          if [ -f biovalidator.pid ]; then
            # If it was already killed, exit with true
            kill "$(cat biovalidator.pid)" || true
          fi

      - name: Fail if a validation suite failed
        if: always()
        run: |
          test "${{ steps.validate_entity_examples.outcome }}" = success
          test "${{ steps.validate_graph_examples.outcome }}" = success
          test "${{ steps.validate_jsonld_frame_round_trips.outcome }}" = success
          test "${{ steps.validate_graph_jsonld_frame_round_trips.outcome }}" = success
